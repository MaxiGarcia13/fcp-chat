---
import { ClientRouter } from 'astro:transitions';
import Breadcrumbs from '@/components/breadcrumbs.astro';
import { cn } from '@/utils/classes';
import '@/styles/global.css';
import pkg from '../../package.json';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;

const repoUrl =
  typeof pkg.repository === 'object' && pkg.repository?.url
    ? pkg.repository.url.replace(/^git\+/, '').replace(/\.git$/, '')
    : (pkg.homepage?.split('#')[0] ?? '#');

const authorLinkedInUrl = 'https://www.linkedin.com/in/maximilianogarcia13';

const description =
  'FCP Chat - Chat para armar los equipos para la proxima fecha de la Liga Cántabra de Pádel';

const pathname = Astro.url.pathname;
const isHomePage = pathname === '/';
---

<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="canonical" href={Astro.url.href} />

    <title>FCP Chat</title>

    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.png" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:title" content="FCP Chat" />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL('/og-image.png', Astro.url)} />
    <meta property="og:locale" content="es_ES" />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={Astro.url.href} />
    <meta property="twitter:title" content="FCP Chat" />
    <meta property="twitter:description" content={description} />
    <meta
      property="twitter:image"
      content={new URL('/og-image.png', Astro.url)}
    />

    <ClientRouter />
  </head>
  <body class="min-h-screen bg-cantabria-bg text-cantabria-text antialiased">
    <main
      class={cn(
        'mx-auto max-w-4xl px-4 py-8 sm:py-10 min-h-screen flex flex-col',
        className,
      )}
    >
      {!isHomePage && <Breadcrumbs pathname={pathname} />}
      <slot />
      <footer
        class="mt-auto border-t border-cantabria-border/40 pt-6 text-center text-xs text-cantabria-muted"
      >
        <p>
          v{pkg.version}
          <span class="mx-1.5 opacity-50" aria-hidden="true">·</span>
          <a
            href={repoUrl}
            class="text-cantabria-link underline-offset-2 hover:underline"
            target="_blank"
            rel="noopener noreferrer"
          >
            Repositorio
          </a>
          <span class="mx-1.5 opacity-50" aria-hidden="true">·</span>
          Creado por{' '}
          <a
            href={authorLinkedInUrl}
            class="text-cantabria-link underline-offset-2 hover:underline"
            target="_blank"
            rel="noopener noreferrer"
          >
            {pkg.author}
          </a>
        </p>
      </footer>
    </main>
  </body>
</html>
